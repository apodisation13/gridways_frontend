services:
  frontend:
    image: ${REGISTRY:-reg}/${FRONTEND_IMAGE_NAME:-front}:${MODE:-latest}
    container_name: gridways-frontend
    restart: unless-stopped
    build:
      context: .
      args:
        - VUE_APP_CUSTOM_ENV=${VUE_APP_CUSTOM_ENV}
        - VUE_APP_DOMAIN=${VUE_APP_DOMAIN}
        - VUE_APP_TG=${VUE_APP_TG}
        - VUE_APP_EMAIL=${VUE_APP_EMAIL}
        - VUE_APP_INN=${VUE_APP_INN}
        - VUE_APP_PHONE=${VUE_APP_PHONE}
    networks:
      - app-network
    ports:
      - "8080:8080"


networks:
  app-network:
    external: true